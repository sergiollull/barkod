package com.example.barkod;

import com.journeyapps.barcodescanner.CaptureActivity;

public class Capture extends CaptureActivity {
    //we don't need to add anything here, because this class can build everything.
}
